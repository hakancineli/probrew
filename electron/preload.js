
// Preload script: used to expose native APIs to the renderer process
// In the future, we can add bridge for thermal printers, etc.
window.addEventListener('DOMContentLoaded', () => {
    // Basic desktop environment indicators
    console.log('ProBrew POS Desktop Environment Loaded');
});
